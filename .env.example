# Copy this file to .env and replace the placeholder with your Prior Labs API key.
# Never commit the populated .env file.
TABPFN_TOKEN=replace_with_your_prior_labs_api_key

# Optional overrides for the isolated original TabPFN v1 backend.
# TABPFN_V1_RUNTIME=.tabpfn-v1-runtime
# TABPFN_V1_CHECKPOINT=/absolute/path/to/prior_diff_real_checkpoint_n_0_epoch_100.cpkt
